import type { NextPage } from "next";
import { ReactElement } from "react";
import Landing from "@/modules/Assigment";

const Index: NextPage = (): ReactElement => {
  return <Landing />;
};

export default Index;
