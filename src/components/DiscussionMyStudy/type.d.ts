import { StaticImageData } from "next/image";

export interface cardAuthor {
    title: string,
    avatar: StaticImageData,
    time: string,
    content: string,
    author: string,
    role: string,
    
}